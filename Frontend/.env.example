# Copy to .env.local to override locally (optional)
# MUST end with /api/users — not the server root (/)
# Wrong: https://api.yourdomain.com
# Right:  https://api.yourdomain.com/api/users
VITE_API_URL=http://100.29.211.141:5001/api/users
